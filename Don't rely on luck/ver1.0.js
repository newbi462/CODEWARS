var guess = 42;// if going to hack it might as well make it the answer to everything

var Math = {
	random: function(){
		return 0;// new Math.random()
	},
	floor: function(){
 		return 42;// no luck when you force the awnser, new Math.floor()
	},
}
